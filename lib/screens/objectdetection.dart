import 'package:flutter/material.dart';

class ObjectDetection extends StatelessWidget {
  @override
  Widget build(BuildContext context) => Scaffold(
        body: Center(child: Text('Object Detection', style: TextStyle(fontSize: 10))),
      );
}
